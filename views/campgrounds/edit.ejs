<!DOCTYPE html>
<html lang="en">

<% layout("layouts/boilerplate") %>

    <h1>Edit <%=camp.title%>
    </h1>
    <section>
        <form action="/campgrounds/<%=camp._id%>?_method=PUT" method="POST">
            <label for="title">Title</label>
            <input type="text" name="title" id="title" placeholder="Title" value="<%= camp.title %>">
            <br>
            <label for="location">Location</label>
            <input type="text" name="location" placeholder="location" id="location" value="<%= camp.location%>">
            <br>
            <label for="price">Price</label>
            <input type="number" name="price" id="price" value="<%= camp.price %>">
            <br>
            <label for="description">Description</label>
            <textarea name="description" id="description" cols="30" rows="10"><%=camp.description%> </textarea>
            <button type="submit">Edit Camp</button>
        </form>

        <a href="/campgrounds">Back to campgrounds</a>
        <br>
        <a href="/campgrounds/<%=camp._id%>"> back to <%=camp.title%> </a>
    </section>